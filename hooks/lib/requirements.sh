# The commands a user runs in their own terminal to satisfy a requirement.
# install.sh renders the whole requirement list and hooks/update.sh reports the
# one that stops updates, so the text lives here and neither owns it.
#
# Sourced, never run.

GH_LOGIN="gh auth login --hostname github.com --git-protocol ssh --web"
