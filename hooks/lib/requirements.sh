# install.sh renders the whole requirement list and hooks/update.sh reports the
# one that stops updates, so this text lives where neither of them owns it.

GH_LOGIN="gh auth login --hostname github.com --git-protocol ssh --web"
